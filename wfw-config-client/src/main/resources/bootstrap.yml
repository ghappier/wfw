
spring:
  application:
    name: config-client
  cloud:
    config:
      name: neo-config
      profile: dev
      label: master
      uri: http://localhost:8005/
    
server:
  port: 8006
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/